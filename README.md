# Telegram Torrent Drop

Telegram-бот для Synology Download Station.

Принимает `.torrent`-файлы и magnet-ссылки из разрешённых Telegram-чатов и добавляет задачи в Download Station через Synology API. Поддерживает поиск по Rutracker.org прямо из чата, поиск и расширение результатов через Jackett (все настроенные индексеры), а также поиск по ссылке с Кинопоиска.

---

## Возможности

- Добавление задач в Download Station из `.torrent`-файла или magnet-ссылки
- Поиск торрентов на Rutracker.org с фильтрами по качеству, звуку и субтитрам
- **Поиск через Jackett** — самостоятельный поиск или расширение результатов Rutracker по всем настроенным индексерам (RuTor, NNMClub, BigFanGroup и др.) с фильтрацией по трекеру, слиянием результатов и отображением источника `[RT]`, `[NNM]`, `[BFG]`…
- Поиск по ссылке Кинопоиска — бот сам получает название, год и режиссёра и формирует поисковый запрос
- Просмотр загрузок через `/status` с постраничной навигацией
- Авто-обновление панели загрузок каждые 30 секунд при активных задачах
- Уведомления о завершении, раздаче или ошибке загрузки (опционально с настраиваемой кнопкой **▶️ Открыть Plex**, по умолчанию выключена)
- Авто-добавление публичных трекеров к BT-задачам
- Автоматическое удаление завершённых задач по таймеру
- Управление доступом пользователей: одобрение запросов, просмотр списка, удаление
- Админ-панель `/admin` с главной сводкой, диагностикой, пользователями и быстрым переходом к загрузкам
- Повтор запросов к Download Station API при временных сбоях сети

---

## Файлы проекта

| Файл | Назначение |
|---|---|
| `bot.py` | Telegram-обработчики и связка модулей |
| `access_control.py` | Правила доступа, администраторы и одобренные пользователи |
| `app_context.py` | Создание клиентов и общего контекста приложения |
| `config.py` | Разбор и валидация переменных окружения |
| `diagnostics.py` | Единая диагностика внешних сервисов для админ-панели |
| `formatters.py` | Функции форматирования сообщений |
| `keyboards.py` | Построители Inline-клавиатур и константы callback-данных |
| `download_station.py` | Клиент Synology Download Station API |
| `state_store.py` | Хранение состояния в JSON-файлах |
| `task_policies.py` | Правила уведомлений и автоудаления задач |
| `task_views.py` | Форматирование списков/карточек задач Download Station |
| `torrent_utils.py` | Работа с magnet-ссылками, `.torrent`-файлами и bencode |
| `tracker_service.py` | Публичные трекеры: загрузка, кэш, дедупликация и добавление в BT-задачи |
| `rutracker.py` | Клиент поиска и загрузки с Rutracker.org |
| `jackett.py` | Клиент Jackett (поиск по внешним индексерам) |
| `kinopoisk.py` | Клиент Кинопоиска (kinopoiskapiunofficial.tech) |
| `Dockerfile` | Сборка образа контейнера |
| `compose.yaml` | Конфигурация для Synology Container Manager |
| `requirements.txt` | Python-зависимости |
| `.env.example` | Пример файла с настройками |

---

## Установка

1. Скопируй `.env.example` в `.env` и заполни необходимые переменные.
2. Положи все файлы проекта в `/volume1/docker/tg_torrent_drop` на NAS.
3. В **Synology Container Manager** → **Проекты** → создай проект из этой папки.
4. Нажми **Собрать** → **Запустить**.

При обновлении: замени изменённые файлы и выполни **Остановить → Собрать → Запустить**.

---

## Настройка

### Обязательные параметры

```env
BOT_TOKEN=123456:ваш_токен
ALLOWED_CHAT_IDS=123456789
DS_URL=https://192.168.1.103:5001
DS_ACCOUNT=tg_bot_ds
DS_PASSWORD=ваш_пароль
DS_DESTINATION=video
```

`ALLOWED_CHAT_IDS` — один или несколько chat_id через запятую:
```env
ALLOWED_CHAT_IDS=123456789,987654321
```

Узнать свой chat_id: написать `/id` боту.

`DS_DESTINATION` — имя общей папки без `/volume1/`. Для `/volume1/video` пишем `video`.

---

### Пользователь Download Station

Создай отдельного пользователя Synology (например, `tg_bot_ds`) и выдай ему:
- доступ к DSM;
- доступ к Download Station;
- права чтения/записи на папку назначения;
- папку назначения по умолчанию в Download Station.

`DS_VERIFY_SSL=false` — практичный режим для домашней Synology с самоподписанным сертификатом: контейнер не будет проверять TLS-сертификат DSM и соединение не сломается из-за локального self-signed сертификата.

Если DSM доступен через публичный домен, reverse proxy или другой недоверенный сегмент сети, лучше выпустить доверенный сертификат и поставить:

```env
DS_VERIFY_SSL=true
```

---

### Администраторы и доступ

```env
ADMIN_CHAT_IDS=123456789
ACCESS_APPROVALS_ENABLED=true
```

`ADMIN_CHAT_IDS` — если не задан, администраторами считаются все из `ALLOWED_CHAT_IDS`.

При `ACCESS_APPROVALS_ENABLED=true` новый пользователь пишет `/start`. Бот отправляет администратору запрос с кнопками **✅ Разрешить** / **🚫 Отклонить**. После одобрения chat_id сохраняется в постоянное хранилище Docker-тома — редактировать `.env` не нужно.

Команда `/users` (только для администратора) показывает всех пользователей с доступом: администраторов, постоянных (из конфига), одобренных — с именем и датой выдачи доступа. Одобренных можно удалить кнопкой прямо из списка.

---

### Часовой пояс и лимиты

```env
BOT_TIMEZONE=Europe/Moscow
MAX_TORRENT_FILE_MB=20
STATE_DIR=/data
```

`BOT_TIMEZONE` — часовой пояс для времени в сообщениях бота.

`MAX_TORRENT_FILE_MB` — максимальный размер `.torrent`-файла, принимаемого от пользователя.

`STATE_DIR=/data` — директория для хранения JSON-файлов состояния. Должна быть смонтирована как volume (по умолчанию `tg_torrent_drop_state`).

---

### Публичные трекеры

```env
TRACKERS_MODE=auto
TRACKERS_URL=https://raw.githubusercontent.com/ngosang/trackerslist/master/trackers_best.txt
TRACKERS_MAX=20
TRACKERS_CACHE_TTL_HOURS=24
TRACKERS_BACKGROUND_ENABLED=true
TRACKERS_BACKGROUND_INTERVAL_SECONDS=180
```

`TRACKERS_MODE=auto` — автоматически добавлять публичные трекеры к новым BT-задачам. Существующие трекеры сохраняются, дубли пропускаются, приватные `.torrent`-файлы не затрагиваются.

При `TRACKERS_BACKGROUND_ENABLED=true` фоновый монитор сканирует активные BT-задачи каждые `TRACKERS_BACKGROUND_INTERVAL_SECONDS` секунд. Кнопка **➕ Добавить трекеры** показывается только для незавершённых BT-задач, которые ещё не были обработаны.

---

### Уведомления

```env
TASK_NOTIFICATIONS_ENABLED=true
TASK_NOTIFICATION_STATUSES=finished,seeding,error
TASK_NOTIFY_EXTERNAL_TASKS=false
NOTIFY_CHAT_IDS=
```

Бот присылает уведомление, когда задача достигает одного из статусов в `TASK_NOTIFICATION_STATUSES`. Статус `seeding` включён по умолчанию, так как для BT-задач означает завершение скачивания. Уведомление отправляется тому, кто создал задачу. Задачи, созданные не через бота, игнорируются, если не задан `NOTIFY_CHAT_IDS` или `TASK_NOTIFY_EXTERNAL_TASKS=true`.

---

### Автоочистка

```env
AUTO_DELETE_FINISHED_AFTER_HOURS=24
AUTO_DELETE_FINISHED_STATUSES=finished
```

Задачи в статусах из `AUTO_DELETE_FINISHED_STATUSES` автоматически удаляются из Download Station через `AUTO_DELETE_FINISHED_AFTER_HOURS` часов. Установи `0` для отключения.

Пример — удалять и завершённые, и раздающие:
```env
AUTO_DELETE_FINISHED_STATUSES=finished,seeding
```

---

### Magnet-ссылки и повторы API

```env
MAGNET_POLL_ATTEMPTS=8
MAGNET_POLL_INTERVAL_SECONDS=1.5
DS_RETRY_ATTEMPTS=3
DS_RETRY_DELAY=2.0
```

После добавления magnet-ссылки бот опрашивает Download Station до `MAGNET_POLL_ATTEMPTS` раз с интервалом `MAGNET_POLL_INTERVAL_SECONDS` секунд, чтобы получить ID задачи.

При сетевых сбоях запросы к Download Station API повторяются до `DS_RETRY_ATTEMPTS` раз с задержкой `DS_RETRY_DELAY` секунд.

---

### Поиск на Rutracker.org

```env
RUTRACKER_USERNAME=ваш_логин
RUTRACKER_PASSWORD=ваш_пароль
RUTRACKER_MAX_RESULTS=10
```

Если не настроены ни Rutracker, ни Jackett — поиск отключён. Если настроен только Jackett, бот может искать через Jackett; если настроены оба источника, Rutracker используется первым, а Jackett доступен как расширение или отдельный поиск.

**Поиск без команды.** Когда Rutracker настроен, можно просто написать название прямо в чат — бот сам предложит варианты качества. Команда `/search` тоже работает как альтернативный вход.

**Поток поиска:**

```
Написать название (или /search)
        → [🔍 Искать (1080p)]  или  [⚙️ Расширенный поиск]
```

**Быстрый поиск** — сразу ищет с добавлением `1080p` к запросу.

**Расширенный поиск** — настройка перед запуском:

| Параметр | Варианты |
|---|---|
| Качество | 🎬 4K / 📺 1080p / 📺 720p / 🔍 Любое |
| 🎵 Оригинальная дорожка | вкл/выкл (добавляет `Original` к запросу) |
| 💬 Субтитры | вкл/выкл (добавляет `Sub` к запросу) |

После выбора — список результатов (до 5 на странице) с размером и числом сидеров:

- ⭐ — рекомендованный результат (выбирается автоматически по качеству раздачи: тип релиза, разрешение, число сидеров)
- 🔎 — остальные результаты
- **◀ / ▶** — листать страницы при большом количестве результатов

Нажми на нужный → открывается карточка с обложкой (если доступна на странице раздачи), размером и числом сидеров → **✅ Скачать** — торрент добавляется в Download Station.

В админ-панели `/admin` кнопка **🧭 Диагностика** запускает единую проверку: Download Station, Rutracker, Jackett, Кинопоиск, кэш public-трекеров и состояние Plex-кнопки.

---

### Кинопоиск → автоматический поиск

```env
KINOPOISK_API_KEY=ваш_ключ
```

Бесплатный ключ API: [kinopoiskapiunofficial.tech](https://kinopoiskapiunofficial.tech) → регистрация → раздел API.

Работает только совместно с Rutracker (оба параметра должны быть заданы).

**Как использовать:** вставь ссылку прямо в чат (никаких команд) — бот сам получит название, год и режиссёра, сформирует поисковый запрос и покажет клавиатуру выбора качества.

```
Пользователь: https://www.kinopoisk.ru/film/77044/
Бот: 🎬 Фильм: Интерстеллар
     Interstellar
     📅 Год: 2014
     🎬 Режиссёр: Кристофер Нолан

     🔍 Запрос для поиска: «Интерстеллар 2014»
     [🔍 Искать (1080p)]  [⚙️ Расширенный поиск]  [❌ Отмена]
```

Поддерживаются форматы URL:
- `https://www.kinopoisk.ru/film/12345/`
- `https://www.kinopoisk.ru/series/12345/`
- `https://kp.ru/film/название-12345/`

---

### Подписки на обновления серий

```env
SUBSCRIPTION_CHECK_INTERVAL_HOURS=6
```

Когда результат поиска на Rutracker — неполная раздача (например, `Серии: 1-8 из 10`), бот показывает дополнительную кнопку **🔔 Скачать и следить**.

При нажатии торрент добавляется в Download Station и бот начинает отслеживать тему. Каждые `SUBSCRIPTION_CHECK_INTERVAL_HOURS` часов он проверяет заголовок страницы на Rutracker и при изменении счётчика серий автоматически скачивает обновлённый торрент и присылает уведомление.

**Уведомления:**

```
🔔 Клиника / Сезон 10: новая серия!
Эпизодов: 8 → 9 из 10
Торрент обновлён в Download Station.
[📋 К загрузкам]  [🔕 Отписаться]

🔔 Клиника / Сезон 10: сезон завершён!
Эпизодов: 9 → 10 из 10 ✅
Торрент обновлён в Download Station.
Подписка снята автоматически.
```

Команда `/subs` показывает список активных подписок с кнопками **🔕 Отписаться**.

---

### Jackett (расширенный поиск)

```env
JACKETT_URL=http://192.168.1.103:9117
JACKETT_API_KEY=ваш_ключ
JACKETT_INDEXERS=all
JACKETT_MAX_RESULTS=10
JACKETT_FETCH_LIMIT=50
```

Jackett — опциональный агрегатор торрент-индексеров. Для включения нужно задать и `JACKETT_URL`, и `JACKETT_API_KEY`; если указать только одну переменную, бот остановится при старте с понятной ошибкой. Когда Jackett включён, в результатах поиска Rutracker появляются две дополнительные кнопки:

- **🌐 Расширить поиск (Jackett)** — добавляет к уже найденным результатам Rutracker результаты из выбранных Jackett-индексеров. Перед поиском можно выбрать конкретные трекеры.
- **🔍 Поиск через Jackett** — ищет только через Jackett, заменяя список результатов.

Результаты из разных источников помечаются аббревиатурой трекера: `[RT]` — Rutracker, `[NNM]` — NNMClub, `[BFG]` — BigFanGroup, `[RuTor]` — RuTor и т.д.

`JACKETT_INDEXERS` — список индексеров через запятую или `all` для всех настроенных. По умолчанию выбирается Rutracker, если он есть в Jackett.

`JACKETT_FETCH_LIMIT` — сколько результатов запросить у Jackett (до обрезки до `JACKETT_MAX_RESULTS`).

В общей диагностике админ-панели Jackett отображается отдельным блоком: бот проверяет API и показывает список доступных индексеров.

---

### Plex

```env
PLEX_ENABLED=false
PLEX_URL=https://app.plex.tv
```

По умолчанию Plex-кнопка выключена: `PLEX_ENABLED=false`.

При `PLEX_ENABLED=true` к уведомлению о завершении или начале раздачи добавляется кнопка **▶️ Открыть Plex**. URL кнопки берётся из `PLEX_URL`; по умолчанию это `https://app.plex.tv`.

Если нужен сценарий “на телефоне открыть приложение Plex, а на компьютере открыть веб-Plex”, укажи в `PLEX_URL` свой HTTPS-редирект, например:

```env
PLEX_ENABLED=true
PLEX_URL=https://example.com/plex
```

Такой редирект может на iOS/Android попробовать открыть приложение Plex, а на desktop отправить пользователя на `https://app.plex.tv`. Сам Telegram-бот не видит платформу пользователя и не может надёжно выбрать iOS/Android/Desktop на своей стороне.

---

## Команды бота

| Команда | Кто видит | Описание |
|---|---|---|
| `/start` | все | Приветствие; неизвестным пользователям отправляет запрос администратору |
| `/help` | разрешённые | Список возможностей бота |
| `/id` | все | Показать свой chat_id |
| `/ping` | разрешённые | Проверка связи |
| `/status` | разрешённые | Панель загрузок Download Station |
| `/search` | разрешённые | Альтернативный вход в поиск через настроенные источники (необязателен — достаточно написать название) |
| `/admin` | администраторы | Главная админ-панель: сводка, диагностика, пользователи, загрузки |
| `/users` | администраторы | Управление доступом пользователей |
| `/cancel` | разрешённые | Отмена текущего диалога (например, поиска) |

Отправь `.torrent`-файл или `magnet:`-ссылку — бот добавит задачу в Download Station.

Если настроен Rutracker или Jackett — просто напиши название фильма или сериала прямо в чат, `/search` не нужен.
Если настроен Кинопоиск — вставь ссылку `kinopoisk.ru` — бот сам найдёт название и запустит поиск.

---

## Админ-панель `/admin`

Команда `/admin` открывает компактную панель администратора. Главный экран показывает:

- время бота
- сводку по задачам Download Station: всего, активных, завершённых и с ошибкой
- количество подписок Rutracker и Jackett, а также время следующей проверки, если оно известно
- состояние уведомлений и автоудаления
- какие интеграции настроены: Rutracker, Jackett, Кинопоиск, Plex, public-трекеры
- текущие параметры поиска по умолчанию

Кнопки панели:

- **🔄 Обновить** — перечитать главную сводку
- **🧭 Диагностика** — открыть полную диагностику Download Station, Rutracker, Jackett, Кинопоиска, public-трекеров и Plex
- **👥 Пользователи** — открыть управление доступом
- **📋 Загрузки** — перейти к общей панели загрузок

Диагностика доступна из панели, отдельной команды для неё больше нет.

---

## Панель загрузок `/status`

Бот держит одну панель на чат. При повторном `/status` старая панель удаляется.

**Кнопки управления:**

- **🔄 Обновить** — перечитать список задач
- **🙋 Мои загрузки / 🌐 Все загрузки** — переключатель для администратора
- **◀ / ▶** — навигация по страницам при большом количестве задач
- **🔎 Задача** → карточка задачи:
  - **🔄 Обновить статус** — обновить карточку
  - **▶️ Запустить / ⏸️ Пауза** — управление задачей
  - **➕ Добавить трекеры** — добавить публичные трекеры (BT-задачи)
  - **🗑️ Удалить** — удалить задачу с подтверждением
  - **📋 К списку загрузок** — вернуться в панель
- **🧹 Удалить завершенные** — удалить все завершённые задачи с подтверждением

Панель автоматически обновляется каждые 30 секунд, пока есть активные загрузки.

---

## Разработка и тесты

```bash
python -m unittest discover -s tests -v
```

Тесты покрывают:
- разбор конфигурации и валидацию переменных
- соответствие `.env.example` и `compose.yaml` (все переменные должны быть в обоих файлах)
- хранилище состояния: roundtrip, атомарная запись, обратная совместимость форматов, обрезка при превышении лимита
- фоновые функции: трекеры, автоудаление, уведомления
- блокировки общих HTTP-сессий клиентов
- единую диагностику в админ-панели
- админ-панель `/admin`
- диагностику Jackett, включая повторную проверку временного startup-ответа
- правила доступа, политики уведомлений, отображение задач и torrent/magnet-утилиты
